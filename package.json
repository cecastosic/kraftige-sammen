{
  "name": "websdk-cdn",
  "version": "1.7.6",
  "description": "Zoom sample app for web client WebSDK",
  "main": "CDN/index.js",
  "dependencies": {
    "concurrently": "^3.5.1",
    "http-server": "^0.12.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "production": "http-server CDN -a 0.0.0.0 -c-1 -o index.html",
    "start": "http-server CDN -a 0.0.0.0 -p 9999 -c-1 -o index.html",
    "https": "http-server CDN -a 0.0.0.0 -p 9999 -c-1 -S -C localhost.crt -K localhost.key -o https://localhost:9999/index.html"
  },
  "author": "Yang Jack",
  "license": "ISC"
}
